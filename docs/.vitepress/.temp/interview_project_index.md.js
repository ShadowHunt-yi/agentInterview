import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agent Copilot 项目实战复盘","description":"","frontmatter":{},"headers":[],"relativePath":"interview/project/index.md","filePath":"interview/project/index.md","lastUpdated":null}');
const _sfc_main = { name: "interview/project/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agent-copilot-项目实战复盘" tabindex="-1">Agent Copilot 项目实战复盘 <a class="header-anchor" href="#agent-copilot-项目实战复盘" aria-label="Permalink to &quot;Agent Copilot 项目实战复盘&quot;">​</a></h1><p>这一页总结 <code>nwe.md</code> 里的项目经历，适合回答“你项目怎么做的”“RAG 链路怎么落地”“质量怎么闭环”这类综合追问。</p><h2 id="项目一句话" tabindex="-1">项目一句话 <a class="header-anchor" href="#项目一句话" aria-label="Permalink to &quot;项目一句话&quot;">​</a></h2><p>这是一个面向客服场景的 <strong>Agent Copilot</strong>：AI 不直接替代客服，而是辅助客服完成知识问答、订单查询、写操作确认、质量兜底和转人工。</p><p>核心链路：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>客服输入</span></span>
<span class="line"><span>→ 敏感检测</span></span>
<span class="line"><span>→ 意图分类</span></span>
<span class="line"><span>→ RAG / General / Handoff / Agent 分支</span></span>
<span class="line"><span>→ LLM 生成或工具调用</span></span>
<span class="line"><span>→ HITL 确认</span></span>
<span class="line"><span>→ 质量评估</span></span>
<span class="line"><span>→ 幻觉降级 / 返回答案</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h2 id="langgraph-怎么用" tabindex="-1">LangGraph 怎么用 <a class="header-anchor" href="#langgraph-怎么用" aria-label="Permalink to &quot;LangGraph 怎么用&quot;">​</a></h2><p>项目用 LangGraph 的 <code>StateGraph</code> 把原来 1883 行的 orchestrator 单体流程改成图执行引擎。</p><p>关键点：</p><ul><li>拆成 17 个节点。</li><li>通过 7 个条件路由函数做动态分支。</li><li>用 <code>OrchestratorState</code> 维护 30+ 个中间状态字段。</li><li>节点函数保持纯函数风格，返回 dict 后由 LangGraph merge 到 state。</li><li>通过 <code>_wrap_node</code> 闭包注入 <code>GraphServices</code>，解决节点需要访问 db、llm provider、retriever 等服务的问题。</li><li><code>use_langgraph=True</code> 时走 <code>chat_with_graph()</code>，异常时降级到原命令式 pipeline。</li></ul><p>可以这样答：</p><blockquote><p>我们不是为了追新框架才用 LangGraph，而是因为客服对话链路里有大量条件分支和中间状态。LangGraph 把这些 if/else 变成显式图结构，每个节点可以独立测试，状态也能被 trace 和复盘。</p></blockquote><h2 id="_7-个条件路由" tabindex="-1">7 个条件路由 <a class="header-anchor" href="#_7-个条件路由" aria-label="Permalink to &quot;7 个条件路由&quot;">​</a></h2><table tabindex="0"><thead><tr><th>路由函数</th><th>作用</th></tr></thead><tbody><tr><td><code>route_after_sensitive</code></td><td>敏感拦截后决定结束或继续</td></tr><tr><td><code>route_after_classification</code></td><td>根据 intent 走 handoff / agent / general / knowledge</td></tr><tr><td><code>route_after_cache</code></td><td>缓存命中直接返回，否则继续检索</td></tr><tr><td><code>route_after_llm</code></td><td>LLM 输出后判断是否进入 tool call</td></tr><tr><td><code>route_after_tool_confirm</code></td><td>写工具是否需要 HITL 确认</td></tr><tr><td><code>route_after_info_completion</code></td><td>信息不足时引导补充或继续评估</td></tr><tr><td><code>route_after_quality_detection</code></td><td>高幻觉时降级，否则 finalize</td></tr></tbody></table><h2 id="为什么不是多智能体" tabindex="-1">为什么不是多智能体 <a class="header-anchor" href="#为什么不是多智能体" aria-label="Permalink to &quot;为什么不是多智能体&quot;">​</a></h2><p>项目没有做多个 LLM agent 互相协作，而是做 <strong>人机协作 Copilot</strong>。</p><p>原因：</p><ul><li>客服链路核心是理解意图、检索知识、查工具、生成回答。</li><li>多智能体更适合复杂推理、辩论、规划型任务。</li><li>客服场景真正复杂的地方在 HITL 确认、质量兜底、转人工和并发安全。</li></ul><p>已实现的 agent 相关能力：</p><ul><li>意图分类路由：knowledge / general / handoff / agent。</li><li>Handoff 状态机：bot → queued → agent_assigned → closed。</li><li>写操作确认：查询类工具直接执行，写入类工具展示确认卡片，由客服确认后执行。</li><li>自动转人工：单次高幻觉或连续低质量回答触发建议转人工。</li></ul><h2 id="rag-写入链路" tabindex="-1">RAG 写入链路 <a class="header-anchor" href="#rag-写入链路" aria-label="Permalink to &quot;RAG 写入链路&quot;">​</a></h2><p>写入链路按“先保结构，再切分，再向量化，再入库”处理：</p><ol><li>文档上传后创建 <code>KnowledgeDocument</code>，初始状态为 draft。</li><li>切分策略可配置：recursive、parent_child、paragraph。</li><li><code>StructuredChunker</code> 保留章节路径、表格表头和页码等结构信息。</li><li>批量调用 <code>EmbeddingProvider</code>，并用 Redis 缓存 embedding。</li><li>PostgreSQL + pgvector 存 dense embedding。</li><li><code>tsvector</code> + GIN 索引用于 sparse / full-text 检索。</li><li>SQLite 作为开发降级方案，只做小规模暴力检索。</li></ol><p>切分原则：</p><blockquote><p>不优先按固定 token 硬切，而是优先按语义边界切。段落、换行、句号、感叹号这些边界都找不到时，才用滑动窗口兜底。</p></blockquote><h2 id="rag-检索链路" tabindex="-1">RAG 检索链路 <a class="header-anchor" href="#rag-检索链路" aria-label="Permalink to &quot;RAG 检索链路&quot;">​</a></h2><p>检索链路是 hybrid retrieval：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>query</span></span>
<span class="line"><span>→ query embedding</span></span>
<span class="line"><span>→ dense HNSW 检索</span></span>
<span class="line"><span>→ sparse GIN / BM25 检索</span></span>
<span class="line"><span>→ RRF 融合</span></span>
<span class="line"><span>→ reranker 精排</span></span>
<span class="line"><span>→ parent 内容附着</span></span>
<span class="line"><span>→ LLM context</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>关键实现：</p><ul><li>Dense：pgvector HNSW，余弦距离算子 <code>&lt;=&gt;</code>。</li><li>Sparse：<code>search_vector @@ plainto_tsquery</code>，按 <code>tsrank_cd</code> 排序。</li><li>融合：RRF，默认 <code>rrf_k = 60</code>。</li><li>Rerank：<code>bge-reranker-v2-m3</code>，过滤低于 0.2 的结果。</li><li>Parent-child：child 用于精准召回，parent 用于提供完整上下文，同 parent 去重。</li></ul><h2 id="关键阈值怎么讲" tabindex="-1">关键阈值怎么讲 <a class="header-anchor" href="#关键阈值怎么讲" aria-label="Permalink to &quot;关键阈值怎么讲&quot;">​</a></h2><table tabindex="0"><thead><tr><th>参数</th><th>项目值</th><th>面试解释</th></tr></thead><tbody><tr><td><code>chunk_size</code></td><td>500 字符</td><td>贴近文档平均段落长度，兼顾语义完整和匹配精度</td></tr><tr><td><code>chunk_overlap</code></td><td>100 字符</td><td>约 20%，防止跨块语义断裂</td></tr><tr><td><code>parent_size</code></td><td>1500 字符</td><td>约 3 个 child，给 LLM 足够上下文</td></tr><tr><td><code>child_size</code></td><td>400 字符</td><td>语义更聚焦，适合 embedding 检索</td></tr><tr><td><code>candidate_multiplier</code></td><td>3</td><td>topK × 3 进 reranker，平衡召回和延迟</td></tr><tr><td><code>rerank_threshold</code></td><td>0.2</td><td>过滤明显无关，保留弱相关给 LLM 判断</td></tr><tr><td><code>MAX_CHUNKS_IN_MEMORY</code></td><td>500</td><td>SQLite 降级模式的安全阀</td></tr><tr><td><code>embedding_cache_ttl</code></td><td>7 天</td><td>覆盖常见文档更新周期，避免 Redis 无限增长</td></tr></tbody></table><h2 id="为什么选-bge-m3" tabindex="-1">为什么选 BGE-M3 <a class="header-anchor" href="#为什么选-bge-m3" aria-label="Permalink to &quot;为什么选 BGE-M3&quot;">​</a></h2><p>选择 BGE-M3 的理由：</p><ul><li>支持多语言，适合中文、英文、中英混合 SOP。</li><li>支持 dense、sparse、multi-vector 三种能力。</li><li>dense 维度 1024，和 pgvector HNSW 搭配可接受。</li><li>SiliconFlow 部署成本低，OpenAI 兼容接口方便切换。</li><li>支持长文本，parent-child 场景下 parent 块也在能力范围内。</li></ul><p>追问可以这样答：</p><blockquote><p>当前代码主要用 dense embedding，sparse 侧先用 BM25 / tsvector 兜底；BGE-M3 sparse 是后续可以替换 BM25 的优化点。</p></blockquote><h2 id="为什么用-langgraph-不用-langchain" tabindex="-1">为什么用 LangGraph 不用 LangChain <a class="header-anchor" href="#为什么用-langgraph-不用-langchain" aria-label="Permalink to &quot;为什么用 LangGraph 不用 LangChain&quot;">​</a></h2><p>LangChain 更适合组件封装和线性 chain，LangGraph 更适合有状态、有条件分支的流程编排。</p><p>项目里不用纯 LangChain 的原因：</p><ul><li>LCEL 更适合 <code>prompt | llm | parser</code> 这类线性链。</li><li>项目有 7 个条件分支和 30+ 状态字段，需要图结构。</li><li>LangChain Agent 偏 LLM 自主决策工具，而项目需要确定性 pipeline + HITL 确认。</li><li>自研 retriever 可以直接控制 embedding、HNSW、GIN、RRF、rerank，不被 VectorStore 抽象限制。</li></ul><h2 id="质量闭环怎么做" tabindex="-1">质量闭环怎么做 <a class="header-anchor" href="#质量闭环怎么做" aria-label="Permalink to &quot;质量闭环怎么做&quot;">​</a></h2><p>闭环不是只记录日志，而是包含检测、归因、降级、可视化和上线门禁。</p><p>5 层闭环：</p><ol><li><strong>实时质量评估</strong>：每条回答计算 faithfulness、answer relevance、context precision、context recall 等。</li><li><strong>自动降级</strong>：高幻觉时不返回 LLM 生成答案，改为展示原始检索资料和风险提示。</li><li><strong>反馈归因</strong>：客服低分反馈后，结合 evaluation log 和 event log 归因到幻觉、检索失败或回答不相关。</li><li><strong>监控看板</strong>：Grafana 看聚合指标，Phoenix 看单条 trace。</li><li><strong>Eval Harness</strong>：固定测试集 + 质量门禁，模型或参数改动后跑回归。</li></ol><p>门禁阈值示例：</p><table tabindex="0"><thead><tr><th>指标</th><th>阈值</th><th>含义</th></tr></thead><tbody><tr><td><code>hallucination_high_rate</code></td><td>≤ 10%</td><td>客服场景对高幻觉容忍度低</td></tr><tr><td><code>retrieval_hit_rate</code></td><td>≥ 70%</td><td>低于该值说明知识库覆盖不足</td></tr><tr><td><code>must_include_rate</code></td><td>≥ 80%</td><td>关键政策、金额、条款必须命中</td></tr><tr><td><code>avg_faithfulness</code></td><td>≥ 0.7</td><td>回答大部分内容要能在 context 中找到依据</td></tr></tbody></table><p>一句话总结：</p><blockquote><p>闭环的价值不是完全自动修复根因，而是快速发现问题、降低错误影响、定位原因，并在上线前用评测门禁阻止质量回退。</p></blockquote><h2 id="可直接背的项目版回答" tabindex="-1">可直接背的项目版回答 <a class="header-anchor" href="#可直接背的项目版回答" aria-label="Permalink to &quot;可直接背的项目版回答&quot;">​</a></h2><blockquote><p>这个项目是客服 Agent Copilot。架构上我用 LangGraph 把原来 1883 行命令式 orchestrator 拆成 17 个节点和 7 个条件路由，用 StateGraph 维护 30 多个状态字段。知识库侧走 hybrid RAG：文档先按语义边界切分，再做 parent-child，child 用于召回，parent 用于补上下文；检索时 dense 走 pgvector HNSW，sparse 走 tsvector / GIN，RRF 融合后再用 bge-reranker 精排。质量上做了实时评估、幻觉降级、负反馈归因、Grafana/Phoenix 观测和 eval harness 门禁。所以它不是简单的问答 bot，而是一套带人工确认和质量闭环的客服 Copilot。</p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/project/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
