import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RAG 检索增强","description":"","frontmatter":{},"headers":[],"relativePath":"interview/rag/index.md","filePath":"interview/rag/index.md","lastUpdated":1780543958000}');
const _sfc_main = { name: "interview/rag/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rag-检索增强" tabindex="-1">RAG 检索增强 <a class="header-anchor" href="#rag-检索增强" aria-label="Permalink to &quot;RAG 检索增强&quot;">​</a></h1><h2 id="q11-分块的重叠部分怎么设置" tabindex="-1">Q11. 分块的重叠部分怎么设置 <a class="header-anchor" href="#q11-分块的重叠部分怎么设置" aria-label="Permalink to &quot;Q11. 分块的重叠部分怎么设置&quot;">​</a></h2><p>重叠是为了避免重要信息刚好被切在两个 chunk 边界。常见设置：</p><ul><li>普通知识库：overlap 取 chunk 长度的 10%-20%。</li><li>结构化文档：按标题、段落、表格边界切，overlap 可以小一些。</li><li>长依赖文档：适当提高 overlap，但要控制重复召回。</li></ul><p>示例：子块 768 token 时，overlap 可从 80-128 token 起步，再通过评测调。</p><h2 id="q12-子块长度对检索效果有什么影响" tabindex="-1">Q12. 子块长度对检索效果有什么影响 <a class="header-anchor" href="#q12-子块长度对检索效果有什么影响" aria-label="Permalink to &quot;Q12. 子块长度对检索效果有什么影响&quot;">​</a></h2><p>子块太短：</p><ul><li>语义不完整，embedding 表达不稳定。</li><li>召回很精确，但给 LLM 的上下文不足。</li></ul><p>子块太长：</p><ul><li>包含多个主题，向量语义被稀释。</li><li>召回结果看似相关，实际答案位置不突出。</li></ul><p>所以 chunk size 是准确率和上下文完整性的平衡。工程上常用父子块：<strong>小块用于召回，大块用于回答</strong>。</p><h2 id="q13-bge-m3-的-m-指什么" tabindex="-1">Q13. BGE-M3 的 M 指什么 <a class="header-anchor" href="#q13-bge-m3-的-m-指什么" aria-label="Permalink to &quot;Q13. BGE-M3 的 M 指什么&quot;">​</a></h2><p>BGE-M3 中的 M 通常指多能力组合，重点是：</p><ul><li><strong>Multilingual</strong>：多语言。</li><li><strong>Multi-granularity</strong>：多粒度，支持短文本到长文档。</li><li><strong>Multi-functionality</strong>：多功能，支持 dense、sparse、multi-vector 表示。</li></ul><p>面试里可以说：BGE-M3 不只是一个普通 dense embedding 模型，它同时支持稠密检索、稀疏检索和多向量检索。</p><h2 id="q14-子块长度为什么设为-768-token" tabindex="-1">Q14. 子块长度为什么设为 768 token <a class="header-anchor" href="#q14-子块长度为什么设为-768-token" aria-label="Permalink to &quot;Q14. 子块长度为什么设为 768 token&quot;">​</a></h2><p>768 token 是一个工程折中值，不是硬性标准：</p><ul><li>比 256/512 更容易保留完整段落和上下文。</li><li>比 1024/1536 更不容易混入多个主题。</li><li>对 BGE-M3 这类支持长文本的模型来说，768 在能力范围内。</li><li>对 reranker 和 LLM context 成本也相对可控。</li></ul><p>更严谨的说法：</p><blockquote><p>我会把 768 当作初始值，通过检索 hit rate、MRR、answer faithfulness 和人工 case 分析调参，而不是迷信固定长度。</p></blockquote><h2 id="q15-父块一般包含多少子块合适" tabindex="-1">Q15. 父块一般包含多少子块合适 <a class="header-anchor" href="#q15-父块一般包含多少子块合适" aria-label="Permalink to &quot;Q15. 父块一般包含多少子块合适&quot;">​</a></h2><p>父块通常包含 3-5 个子块比较稳：</p><ul><li>太少：上下文补充不明显。</li><li>太多：返回给 LLM 的内容噪声变多。</li><li>表格、制度、SOP 类文档可以按一个章节或一个完整业务规则作为父块。</li></ul><p>实践上可以让 parent 控制在 1500-3000 token，child 控制在 512-768 token。</p><h2 id="q16-稀疏向量和稠密向量怎么检索" tabindex="-1">Q16. 稀疏向量和稠密向量怎么检索 <a class="header-anchor" href="#q16-稀疏向量和稠密向量怎么检索" aria-label="Permalink to &quot;Q16. 稀疏向量和稠密向量怎么检索&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类型</th><th>检索方式</th><th>擅长</th></tr></thead><tbody><tr><td>稠密向量</td><td>embedding 后做 ANN 近邻搜索，如 HNSW、IVF、DiskANN</td><td>语义相似、同义表达</td></tr><tr><td>稀疏向量</td><td>词项倒排索引，如 BM25、SPLADE、BGE-M3 sparse</td><td>精确关键词、编号、专有名词</td></tr></tbody></table><p>稠密检索像“理解意思”，稀疏检索像“命中关键词”。RAG 工程里常把两者融合，避免只靠一种召回。</p><h2 id="q17-稀疏和稠密权重怎么设置" tabindex="-1">Q17. 稀疏和稠密权重怎么设置 <a class="header-anchor" href="#q17-稀疏和稠密权重怎么设置" aria-label="Permalink to &quot;Q17. 稀疏和稠密权重怎么设置&quot;">​</a></h2><p>两种常见方式：</p><ol><li>加权分数融合：<code>score = α × dense + (1 - α) × sparse</code>。</li><li>RRF 融合：只看排名，不直接比较分数。</li></ol><p>初始权重可以这样设：</p><ul><li>通用语义问答：dense 0.6-0.7，sparse 0.3-0.4。</li><li>编号、政策条款、字段名多：sparse 提高到 0.5。</li><li>分数不可比或模型来源不同：优先用 RRF。</li></ul><h2 id="q18-bge-m3-除了稀疏和稠密还有什么" tabindex="-1">Q18. BGE-M3 除了稀疏和稠密还有什么 <a class="header-anchor" href="#q18-bge-m3-除了稀疏和稠密还有什么" aria-label="Permalink to &quot;Q18. BGE-M3 除了稀疏和稠密还有什么&quot;">​</a></h2><p>还有 <strong>multi-vector retrieval</strong>。它不是把整段文本压成一个向量，而是保留多个 token-level 或子向量表示，更适合细粒度匹配。</p><p>可以这样答：</p><blockquote><p>BGE-M3 支持 dense、sparse、multi-vector 三种检索能力。dense 做语义召回，sparse 做词项匹配，multi-vector 提升细粒度交互匹配能力，但工程成本和索引复杂度更高。</p></blockquote><h2 id="q19-rag-链路上怎么提升检索速度" tabindex="-1">Q19. RAG 链路上怎么提升检索速度 <a class="header-anchor" href="#q19-rag-链路上怎么提升检索速度" aria-label="Permalink to &quot;Q19. RAG 链路上怎么提升检索速度&quot;">​</a></h2><p>可优化位置：</p><table tabindex="0"><thead><tr><th>链路</th><th>优化</th></tr></thead><tbody><tr><td>Query embedding</td><td>缓存高频 query embedding</td></tr><tr><td>向量检索</td><td>HNSW / IVF 参数调优，控制 topK</td></tr><tr><td>混合检索</td><td>dense 和 sparse 并行查询</td></tr><tr><td>Rerank</td><td>只 rerank 粗召回 top 20-50</td></tr><tr><td>上下文构建</td><td>父块批量查询，去重和裁剪</td></tr><tr><td>服务层</td><td>连接池、批处理、异步并发</td></tr></tbody></table><p>关键思路：召回阶段快而宽，重排阶段慢而窄。</p><h2 id="q20-为什么不用-rerank-模型代替混合检索" tabindex="-1">Q20. 为什么不用 rerank 模型代替混合检索 <a class="header-anchor" href="#q20-为什么不用-rerank-模型代替混合检索" aria-label="Permalink to &quot;Q20. 为什么不用 rerank 模型代替混合检索&quot;">​</a></h2><p>Reranker 通常是 cross-encoder，需要把 query 和每个候选文档拼在一起输入模型。它精度高，但不能直接在全库上跑。</p><p>混合检索负责从全库快速找候选，rerank 负责对小候选集精排。两者是上下游关系，不是替代关系。</p><h2 id="q30-为什么-rag-召回比重排快" tabindex="-1">Q30. 为什么 RAG 召回比重排快 <a class="header-anchor" href="#q30-为什么-rag-召回比重排快" aria-label="Permalink to &quot;Q30. 为什么 RAG 召回比重排快&quot;">​</a></h2><p>从模型架构看：</p><ul><li><strong>Bi-encoder 召回</strong>：query 和 document 分别编码，文档向量可离线预计算；在线只算 query 向量，再做向量近邻。</li><li><strong>Cross-encoder 重排</strong>：query 和 document 拼接后一起过模型，每个候选都要跑一次前向。</li></ul><p>所以 bi-encoder 快、适合全库召回；cross-encoder 慢、适合 topK 精排。</p><h2 id="q31-milvus-用什么索引-为什么选-hnsw" tabindex="-1">Q31. Milvus 用什么索引，为什么选 HNSW <a class="header-anchor" href="#q31-milvus-用什么索引-为什么选-hnsw" aria-label="Permalink to &quot;Q31. Milvus 用什么索引，为什么选 HNSW&quot;">​</a></h2><p>HNSW 是图索引，通过多层近邻图做近似最近邻搜索。选择它的理由：</p><ul><li>召回上限高，调参后质量好。</li><li>小到中等规模数据速度很快。</li><li>参数直观：<code>M</code> 控制图连接数，<code>efConstruction</code> 控制建图质量，<code>efSearch</code> 控制查询质量。</li></ul><p>取舍：</p><ul><li>HNSW 内存占用较高。</li><li>数据量极大或强磁盘约束时，可以考虑 IVF、DiskANN 等。</li></ul><p>面试话术：</p><blockquote><p>我选 HNSW 是因为项目数据规模不算超大，更看重召回质量和调参可控性。它有三档旋钮：M、efConstruction、efSearch，可以在内存、构建时间、召回率和查询延迟之间做平衡。</p></blockquote><h2 id="rag-深挖追问排查" tabindex="-1">RAG 深挖追问排查 <a class="header-anchor" href="#rag-深挖追问排查" aria-label="Permalink to &quot;RAG 深挖追问排查&quot;">​</a></h2><h3 id="分块不是越大越好-也不是越小越好" tabindex="-1">分块不是越大越好，也不是越小越好 <a class="header-anchor" href="#分块不是越大越好-也不是越小越好" aria-label="Permalink to &quot;分块不是越大越好，也不是越小越好&quot;">​</a></h3><p>分块本质是在两个目标之间折中：</p><table tabindex="0"><thead><tr><th>目标</th><th>倾向</th></tr></thead><tbody><tr><td>检索精确</td><td>chunk 小，主题单一</td></tr><tr><td>上下文完整</td><td>chunk 大，信息更全</td></tr></tbody></table><p>如果面试官追问“为什么不用固定 768 token”，要回答：</p><blockquote><p>768 只是初始经验值，最终要通过 eval 调。不同文档类型差异很大，制度文档适合按条款，表格适合按表头和行块，FAQ 适合按问答对，代码文档适合按函数或模块边界。</p></blockquote><p>调参指标：</p><ul><li><code>retrieval_hit_rate</code>：是否召回到正确文档。</li><li><code>MRR</code>：正确文档排得是否靠前。</li><li><code>context_precision</code>：召回上下文噪声多不多。</li><li><code>faithfulness</code>：答案是否基于上下文。</li><li>人工 bad case：看失败是切分、召回、重排还是生成问题。</li></ul><h3 id="overlap-怎么避免重复污染" tabindex="-1">overlap 怎么避免重复污染 <a class="header-anchor" href="#overlap-怎么避免重复污染" aria-label="Permalink to &quot;overlap 怎么避免重复污染&quot;">​</a></h3><p>overlap 能解决跨块断裂，但也会带来重复内容：</p><ul><li>同一句话可能出现在两个 chunk。</li><li>Rerank 可能把重复 chunk 都排前面。</li><li>LLM context 被重复信息占满，浪费 token。</li></ul><p>工程上要做：</p><ul><li>chunk 级去重：相同 text hash 不重复入库。</li><li>parent 级去重：命中多个 child 时，同一个 parent 只放一次。</li><li>context 构建裁剪：按 token budget 控制上下文长度。</li></ul><p>一句话：</p><blockquote><p>overlap 是为了召回不断裂，去重和裁剪是为了上下文不膨胀。</p></blockquote><h3 id="rrf-为什么适合混合检索" tabindex="-1">RRF 为什么适合混合检索 <a class="header-anchor" href="#rrf-为什么适合混合检索" aria-label="Permalink to &quot;RRF 为什么适合混合检索&quot;">​</a></h3><p>Dense 和 sparse 的分数尺度不一致：</p><ul><li>dense 可能是 cosine similarity。</li><li>sparse 可能是 BM25 / tsrank。</li><li>reranker 又是另一个 relevance score。</li></ul><p>如果直接加权，分数很难校准。RRF 只看排名：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>score += 1 / (k + rank)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>好处：</p><ul><li>不要求 dense 和 sparse 分数同尺度。</li><li>对 top 排名更敏感。</li><li>实现简单，稳定性好。</li></ul><p>追问回答：</p><blockquote><p>如果有大量标注数据，可以学习融合权重；没有足够标注时，RRF 是更稳的工程选择。</p></blockquote><h3 id="rerank-为什么不能替代召回" tabindex="-1">rerank 为什么不能替代召回 <a class="header-anchor" href="#rerank-为什么不能替代召回" aria-label="Permalink to &quot;rerank 为什么不能替代召回&quot;">​</a></h3><p>Reranker 通常是 cross-encoder，它要把 query 和 document 拼起来一起算：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[query, document] → model → relevance score</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这意味着它无法对全库每个文档都跑一遍。召回必须先用更快的 bi-encoder / BM25 从全库缩小候选集。</p><p>面试要强调：</p><blockquote><p>召回解决“从百万文档里找几十个候选”，rerank 解决“从几十个候选里排出最相关的几个”。两者不是替代关系。</p></blockquote><h3 id="hnsw-三个参数怎么讲" tabindex="-1">HNSW 三个参数怎么讲 <a class="header-anchor" href="#hnsw-三个参数怎么讲" aria-label="Permalink to &quot;HNSW 三个参数怎么讲&quot;">​</a></h3><table tabindex="0"><thead><tr><th>参数</th><th>含义</th><th>调大影响</th></tr></thead><tbody><tr><td><code>M</code></td><td>每个节点连接的邻居数</td><td>召回率更高，内存更多</td></tr><tr><td><code>efConstruction</code></td><td>建图时搜索范围</td><td>索引质量更高，建图更慢</td></tr><tr><td><code>efSearch</code></td><td>查询时搜索范围</td><td>召回率更高，查询更慢</td></tr></tbody></table><p>可以这样答：</p><blockquote><p>如果线上发现召回质量不够，优先调大 efSearch，因为它不需要重建索引；如果整体索引质量不够，再考虑调 M 和 efConstruction，但这会增加内存和建图成本。</p></blockquote><h3 id="bge-m3-的-three-modes-怎么落地" tabindex="-1">BGE-M3 的 three modes 怎么落地 <a class="header-anchor" href="#bge-m3-的-three-modes-怎么落地" aria-label="Permalink to &quot;BGE-M3 的 three modes 怎么落地&quot;">​</a></h3><table tabindex="0"><thead><tr><th>模式</th><th>落地方式</th><th>成本</th></tr></thead><tbody><tr><td>Dense</td><td>存向量库，ANN 检索</td><td>工程最常见</td></tr><tr><td>Sparse</td><td>存稀疏 token 权重，倒排检索</td><td>需要 sparse index 支持</td></tr><tr><td>Multi-vector</td><td>一个文档多个向量做细粒度匹配</td><td>精度高但索引和算力成本更高</td></tr></tbody></table><p>面试里要诚实：</p><blockquote><p>如果项目只用了 dense + BM25，也可以说 BGE-M3 sparse/multi-vector 是后续优化方向。不要说已经用了但解释不清怎么存、怎么查。</p></blockquote><h3 id="rag-追问清单" tabindex="-1">RAG 追问清单 <a class="header-anchor" href="#rag-追问清单" aria-label="Permalink to &quot;RAG 追问清单&quot;">​</a></h3><table tabindex="0"><thead><tr><th>追问</th><th>回答要点</th></tr></thead><tbody><tr><td>检索失败怎么定位？</td><td>先看 query rewrite，再看召回 topK，再看 rerank，再看 LLM 是否使用 context</td></tr><tr><td>为什么答案有上下文还幻觉？</td><td>可能 context 噪声大、prompt 约束弱、模型没引用证据、答案超出资料</td></tr><tr><td>topK 怎么设？</td><td>召回 topK 可大些，rerank 后给 LLM 的 topK 要受 token budget 控制</td></tr><tr><td>hybrid 权重怎么调？</td><td>关键词强场景提高 sparse，语义问答提高 dense；分数不可靠用 RRF</td></tr><tr><td>父子块为什么有效？</td><td>child 保证精确召回，parent 保证上下文完整</td></tr><tr><td>如何评估 RAG？</td><td>分开评估 retrieval、rerank、generation，不只看最终答案</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/rag/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
