import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Transformer 基础","description":"","frontmatter":{},"headers":[],"relativePath":"interview/transformer/index.md","filePath":"interview/transformer/index.md","lastUpdated":1780543958000}');
const _sfc_main = { name: "interview/transformer/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="transformer-基础" tabindex="-1">Transformer 基础 <a class="header-anchor" href="#transformer-基础" aria-label="Permalink to &quot;Transformer 基础&quot;">​</a></h1><h2 id="q4-讲一下-transformer-原理" tabindex="-1">Q4. 讲一下 Transformer 原理 <a class="header-anchor" href="#q4-讲一下-transformer-原理" aria-label="Permalink to &quot;Q4. 讲一下 Transformer 原理&quot;">​</a></h2><p>Transformer 是一种基于 <strong>自注意力机制</strong> 的序列建模架构。它不依赖 RNN 的逐步递归，而是让序列中每个 token 同时关注其他 token，通过 attention 学到上下文关系。</p><p>核心模块：</p><ol><li><strong>Embedding + Position Encoding</strong>：把 token 变成向量，并注入位置信息。</li><li><strong>Multi-Head Attention</strong>：多组注意力头从不同子空间学习关系。</li><li><strong>Feed Forward Network</strong>：对每个 token 的表示做非线性变换。</li><li><strong>Residual + LayerNorm</strong>：稳定深层训练，缓解梯度问题。</li></ol><p>自注意力计算可以概括为：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Attention(Q, K, V) = softmax(QK^T / sqrt(d_k)) V</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>Q</code> 决定我要找什么，<code>K</code> 决定我能提供什么匹配信号，<code>V</code> 是真正被聚合的信息。</p><h2 id="q5-编码器和解码器的作用区别" tabindex="-1">Q5. 编码器和解码器的作用区别 <a class="header-anchor" href="#q5-编码器和解码器的作用区别" aria-label="Permalink to &quot;Q5. 编码器和解码器的作用区别&quot;">​</a></h2><table tabindex="0"><thead><tr><th>维度</th><th>Encoder</th><th>Decoder</th></tr></thead><tbody><tr><td>输入</td><td>完整输入序列</td><td>已生成 token 或目标序列前缀</td></tr><tr><td>注意力</td><td>双向注意力，可看全局</td><td>因果注意力，只能看当前位置之前</td></tr><tr><td>目标</td><td>理解和表征输入</td><td>自回归生成下一个 token</td></tr><tr><td>典型模型</td><td>BERT、RoBERTa</td><td>GPT、LLaMA、Qwen</td></tr></tbody></table><p>Encoder 擅长理解，Decoder 擅长生成。Encoder-Decoder 则适合翻译、摘要等输入输出都很明确的任务。</p><h2 id="q6-为什么-bert-用编码器-gpt-用解码器" tabindex="-1">Q6. 为什么 BERT 用编码器，GPT 用解码器 <a class="header-anchor" href="#q6-为什么-bert-用编码器-gpt-用解码器" aria-label="Permalink to &quot;Q6. 为什么 BERT 用编码器，GPT 用解码器&quot;">​</a></h2><p>BERT 的目标是做语言理解，例如分类、匹配、抽取。它需要同时看左右上下文，所以采用 Encoder 的双向注意力，并用 MLM 任务训练。</p><p>GPT 的目标是生成文本。生成时只能基于已经生成的内容预测下一个 token，所以采用 Decoder 的因果注意力。</p><p>一句话：</p><blockquote><p>BERT 是“读完整段话再理解”，GPT 是“从左到右一个 token 一个 token 写出来”。</p></blockquote><h2 id="q7-embedding-的底层原理" tabindex="-1">Q7. Embedding 的底层原理 <a class="header-anchor" href="#q7-embedding-的底层原理" aria-label="Permalink to &quot;Q7. Embedding 的底层原理&quot;">​</a></h2><p>Embedding 本质是一个可训练查表矩阵。假设词表大小是 <code>V</code>，隐藏维度是 <code>d</code>，Embedding 矩阵就是 <code>V × d</code>。token id 进来后，相当于取出对应行向量。</p><p>它学习到的是 token 在语义空间中的分布式表示：</p><ul><li>语义相近的词向量距离更近。</li><li>句子 embedding 通常通过池化、CLS token 或专门模型训练得到。</li><li>RAG 里的 embedding 更关注“查询和文档是否语义相关”，通常会用对比学习训练。</li></ul><p>面试追问可以补：</p><blockquote><p>Embedding 不只是词典映射，它是在训练目标约束下形成的连续向量空间。语言模型里的 token embedding 服务于 next token prediction，检索模型里的 embedding 服务于相似度匹配，所以同样叫 embedding，训练目标和使用方式并不完全一样。</p></blockquote><h2 id="q36-layernorm、batchnorm-和-rmsnorm-有什么区别" tabindex="-1">Q36. LayerNorm、BatchNorm 和 RMSNorm 有什么区别 <a class="header-anchor" href="#q36-layernorm、batchnorm-和-rmsnorm-有什么区别" aria-label="Permalink to &quot;Q36. LayerNorm、BatchNorm 和 RMSNorm 有什么区别&quot;">​</a></h2><p>一句话先答：</p><blockquote><p>BatchNorm 按 batch 维度统计，适合 CNN 这类 batch 稳定的场景；LayerNorm 按单个样本的 hidden 维度统计，更适合 Transformer；RMSNorm 是 LayerNorm 的简化版，不减均值，只按均方根缩放，计算更省、在大模型里更常见。</p></blockquote><h3 id="batchnorm-是什么" tabindex="-1">BatchNorm 是什么 <a class="header-anchor" href="#batchnorm-是什么" aria-label="Permalink to &quot;BatchNorm 是什么&quot;">​</a></h3><p>BatchNorm 对一个 batch 内同一通道的激活做归一化。它关心的是“这个通道在一批样本上的分布”。</p><p>简化理解：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BN(x) = gamma * (x - mean_batch) / sqrt(var_batch + eps) + beta</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>特点：</p><ul><li>统计量依赖 batch。</li><li>训练和推理行为不同：推理时用训练阶段累计的 running mean / variance。</li><li>batch 越小，统计越不稳定。</li><li>在 CNN 中很有效，因为图像特征通道有比较稳定的 batch 统计。</li></ul><p>为什么不适合语言模型：</p><ul><li>NLP 序列长度不固定，batch 内 token 分布差异大。</li><li>自回归生成时 batch size 可能很小，甚至为 1。</li><li>推理阶段逐 token 生成，依赖 batch 统计不自然。</li></ul><h3 id="layernorm-是什么" tabindex="-1">LayerNorm 是什么 <a class="header-anchor" href="#layernorm-是什么" aria-label="Permalink to &quot;LayerNorm 是什么&quot;">​</a></h3><p>LayerNorm 对单个 token 的 hidden 维度做归一化。它不看 batch 里的其他样本，只看当前样本自身。</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>LN(x) = gamma * (x - mean_hidden) / sqrt(var_hidden + eps) + beta</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>在 Transformer 中，<code>x</code> 通常是某个 token 的 hidden state，维度是 <code>hidden_size</code>。LayerNorm 会在这个 hidden 向量内部计算均值和方差。</p><p>特点：</p><ul><li>不依赖 batch size。</li><li>训练和推理行为一致。</li><li>适合变长序列和自回归生成。</li><li>能稳定深层网络训练，避免激活尺度在残差连接中不断漂移。</li></ul><p>Transformer 常见结构里有两种放法：</p><table tabindex="0"><thead><tr><th>结构</th><th>位置</th><th>特点</th></tr></thead><tbody><tr><td>Post-LN</td><td><code>x + Sublayer(LN 或 Sublayer 后再 LN)</code></td><td>早期 Transformer 常见，但深层训练更难</td></tr><tr><td>Pre-LN</td><td><code>x + Sublayer(LN(x))</code></td><td>现代大模型常见，梯度更稳定</td></tr></tbody></table><p>面试可补一句：</p><blockquote><p>大模型普遍用 Pre-LN 或类似结构，是因为残差主干更像一条稳定的信息高速路，梯度更容易往前传。</p></blockquote><h3 id="rmsnorm-是什么" tabindex="-1">RMSNorm 是什么 <a class="header-anchor" href="#rmsnorm-是什么" aria-label="Permalink to &quot;RMSNorm 是什么&quot;">​</a></h3><p>RMSNorm 可以看作 LayerNorm 的简化版。它不减均值，只用 root mean square 归一化激活尺度：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RMS(x) = sqrt(mean(x^2) + eps)</span></span>
<span class="line"><span>RMSNorm(x) = gamma * x / RMS(x)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>和 LayerNorm 的关键区别：</p><ul><li>LayerNorm 做 re-centering：减去均值。</li><li>RMSNorm 只做 re-scaling：按均方根缩放。</li></ul><p>为什么现在很多大模型用 RMSNorm：</p><ul><li>少算一个均值中心化步骤，计算更简单。</li><li>对大模型来说，控制激活尺度通常比严格置零均值更关键。</li><li>推理更省一点，尤其在超深层、长序列、高并发下会积累收益。</li><li>LLaMA、Qwen 等 decoder-only 大模型都常用 RMSNorm 或类似变体。</li></ul><h3 id="三者核心对比" tabindex="-1">三者核心对比 <a class="header-anchor" href="#三者核心对比" aria-label="Permalink to &quot;三者核心对比&quot;">​</a></h3><table tabindex="0"><thead><tr><th>维度</th><th>BatchNorm</th><th>LayerNorm</th><th>RMSNorm</th></tr></thead><tbody><tr><td>统计维度</td><td>batch / channel</td><td>单样本 hidden 维度</td><td>单样本 hidden 维度</td></tr><tr><td>是否依赖 batch</td><td>是</td><td>否</td><td>否</td></tr><tr><td>是否减均值</td><td>是</td><td>是</td><td>否</td></tr><tr><td>是否除以方差/尺度</td><td>是</td><td>是</td><td>是，除以 RMS</td></tr><tr><td>训练推理是否一致</td><td>不完全一致</td><td>一致</td><td>一致</td></tr><tr><td>常见场景</td><td>CNN</td><td>Transformer</td><td>现代 LLM</td></tr><tr><td>优点</td><td>利用 batch 统计，CNN 中效果好</td><td>稳定、通用</td><td>更省计算，适合大模型</td></tr></tbody></table><h3 id="延伸追问" tabindex="-1">延伸追问 <a class="header-anchor" href="#延伸追问" aria-label="Permalink to &quot;延伸追问&quot;">​</a></h3><p><strong>Q: 为什么 Transformer 不用 BatchNorm？</strong></p><p>因为 Transformer 的 token 序列长度、padding、mask 和 batch size 都不稳定。BatchNorm 依赖 batch 统计，训练和推理还不一致；自回归生成时逐 token 推理，更不适合依赖 batch 维度。</p><p><strong>Q: LayerNorm 为什么能稳定训练？</strong></p><p>残差连接会不断叠加子层输出，如果激活尺度失控，深层网络很容易梯度不稳定。LayerNorm 把每层输入或输出拉回相对稳定的尺度，让 attention 和 MLP 的输入分布更可控。</p><p><strong>Q: RMSNorm 去掉均值会不会损失能力？</strong></p><p>理论上它少了 re-centering，但实践中大模型更关心激活尺度稳定。残差流和后续线性层仍然有足够表达能力，所以 RMSNorm 往往能在效果接近的情况下减少计算。</p><p><strong>Q: gamma 和 beta 是干什么的？</strong></p><p>归一化会把分布标准化，但模型不一定希望所有层都保持标准分布。<code>gamma</code> 和 <code>beta</code> 是可学习的缩放和平移，让模型自己决定归一化后该恢复到什么尺度。RMSNorm 通常只有 <code>gamma</code>，没有 <code>beta</code>。</p><h2 id="q37-位置编码有什么用-为什么用旋转位置编码" tabindex="-1">Q37. 位置编码有什么用，为什么用旋转位置编码 <a class="header-anchor" href="#q37-位置编码有什么用-为什么用旋转位置编码" aria-label="Permalink to &quot;Q37. 位置编码有什么用，为什么用旋转位置编码&quot;">​</a></h2><p>一句话先答：</p><blockquote><p>Transformer 的 attention 本身不天然知道 token 顺序，所以需要位置编码告诉模型“词在第几个位置”。旋转位置编码 RoPE 把位置信息注入到 Q/K 向量的旋转角度里，使注意力分数天然包含相对位置信息，因此很适合 decoder-only 大模型和长上下文扩展。</p></blockquote><h3 id="为什么需要位置编码" tabindex="-1">为什么需要位置编码 <a class="header-anchor" href="#为什么需要位置编码" aria-label="Permalink to &quot;为什么需要位置编码&quot;">​</a></h3><p>Self-Attention 对输入 token 的处理本质上是集合式的。没有位置编码时，模型只知道有哪些 token，不知道它们的顺序。</p><p>例如：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>我 喜欢 你</span></span>
<span class="line"><span>你 喜欢 我</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这两句话 token 集合相同，但语义不同。如果没有位置信息，attention 很难区分顺序关系。</p><p>位置编码解决两个问题：</p><ul><li><strong>绝对位置</strong>：这个 token 在第几个位置。</li><li><strong>相对位置</strong>：两个 token 相隔多远、谁在谁前面。</li></ul><h3 id="常见位置编码方式" tabindex="-1">常见位置编码方式 <a class="header-anchor" href="#常见位置编码方式" aria-label="Permalink to &quot;常见位置编码方式&quot;">​</a></h3><table tabindex="0"><thead><tr><th>方法</th><th>思路</th><th>优缺点</th></tr></thead><tbody><tr><td>Sinusoidal PE</td><td>用正弦余弦函数生成固定位置向量</td><td>无需训练，可外推，但表达相对朴素</td></tr><tr><td>Learned Absolute PE</td><td>每个位置一个可学习向量</td><td>简单有效，但外推到更长长度较弱</td></tr><tr><td>Relative Position Bias</td><td>attention 分数里加相对距离偏置</td><td>相对位置强，常见于 T5 等</td></tr><tr><td>ALiBi</td><td>根据距离给 attention 加线性偏置</td><td>长度外推友好，简单高效</td></tr><tr><td>RoPE</td><td>旋转 Q/K 向量注入位置</td><td>同时保留绝对位置形式和相对位置效果，大模型常用</td></tr></tbody></table><h3 id="rope-的直觉" tabindex="-1">RoPE 的直觉 <a class="header-anchor" href="#rope-的直觉" aria-label="Permalink to &quot;RoPE 的直觉&quot;">​</a></h3><p>RoPE 的做法是：把 hidden 向量按两两维度分组，每一组看成二维平面上的点，然后根据 token 位置把它旋转一个角度。</p><p>位置越靠后，旋转角度越大：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>q_pos = rotate(q, position)</span></span>
<span class="line"><span>k_pos = rotate(k, position)</span></span>
<span class="line"><span>attention_score = q_pos · k_pos</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>关键点在于：两个旋转后的向量做点积时，结果会和它们的<strong>相对位置差</strong>有关。</p><p>也就是说，RoPE 不只是告诉模型“我在第 10 个位置”，还让模型在 attention 分数里自然感知“我和另一个 token 隔了多少距离”。</p><h3 id="为什么大模型喜欢-rope" tabindex="-1">为什么大模型喜欢 RoPE <a class="header-anchor" href="#为什么大模型喜欢-rope" aria-label="Permalink to &quot;为什么大模型喜欢 RoPE&quot;">​</a></h3><ol><li><p><strong>相对位置建模更自然</strong></p><p>语言任务里，距离关系通常比绝对位置更重要。比如当前 token 更关注前面几个 token，或者关注同一句话里的实体。</p></li><li><p><strong>适合自回归解码</strong></p><p>Decoder-only 模型每一步生成新 token，只需要给新的 Q/K 应用对应位置旋转，和 KV cache 结合方便。</p></li><li><p><strong>不会简单占用 embedding 加法通道</strong></p><p>传统绝对位置编码通常直接加到 token embedding 上。RoPE 是作用在 attention 的 Q/K 上，把位置信息放进注意力匹配过程。</p></li><li><p><strong>长上下文扩展更好做</strong></p><p>RoPE 本身仍然有训练长度限制，但可以通过 NTK scaling、YaRN、位置插值等方法扩展上下文，比 learned absolute position 更容易处理长上下文。</p></li><li><p><strong>工程实现简洁</strong></p><p>只需要对 Q/K 做成对维度旋转，不改 V，不改 attention 主流程。</p></li></ol><h3 id="rope-和绝对位置编码的区别" tabindex="-1">RoPE 和绝对位置编码的区别 <a class="header-anchor" href="#rope-和绝对位置编码的区别" aria-label="Permalink to &quot;RoPE 和绝对位置编码的区别&quot;">​</a></h3><table tabindex="0"><thead><tr><th>维度</th><th>绝对位置编码</th><th>RoPE</th></tr></thead><tbody><tr><td>注入位置</td><td>embedding 层相加</td><td>attention 的 Q/K</td></tr><tr><td>强调信息</td><td>token 的绝对位置</td><td>attention 中的相对距离</td></tr><tr><td>长度外推</td><td>learned PE 较弱</td><td>配合 scaling 更常见</td></tr><tr><td>KV cache</td><td>可用，但不突出</td><td>和 decoder-only 推理更契合</td></tr><tr><td>大模型使用</td><td>早期常见</td><td>现代 LLM 常见</td></tr></tbody></table><h3 id="延伸追问-1" tabindex="-1">延伸追问 <a class="header-anchor" href="#延伸追问-1" aria-label="Permalink to &quot;延伸追问&quot;">​</a></h3><p><strong>Q: 为什么 RoPE 只作用在 Q/K，不作用在 V？</strong></p><p>Attention 分数由 <code>QK^T</code> 决定，位置关系主要影响“该关注谁”。<code>V</code> 是被聚合的内容本身，不负责计算匹配关系，所以一般只旋转 Q/K。</p><p><strong>Q: RoPE 是否真的能无限外推？</strong></p><p>不能。RoPE 比 learned absolute PE 更适合扩展，但模型训练时见过的长度有限，超过训练长度后仍可能退化。实际长上下文模型通常会配合 RoPE scaling、继续训练或长上下文微调。</p><p><strong>Q: RoPE 为什么能表示相对位置？</strong></p><p>因为二维旋转有一个性质：两个分别旋转了位置 <code>m</code> 和 <code>n</code> 的向量做点积时，角度差里会出现 <code>m - n</code>。attention score 因此能感知 token 之间的相对距离。</p><p><strong>Q: 位置编码和 causal mask 是一回事吗？</strong></p><p>不是。位置编码告诉模型顺序和距离；causal mask 限制 decoder 不能看未来 token。一个提供位置信息，一个控制可见范围。</p><p><strong>Q: 面试里怎么简短回答？</strong></p><p>可以这样说：</p><blockquote><p>Attention 本身是 permutation-invariant 的，不加位置编码就不知道 token 顺序。RoPE 把位置变成 Q/K 向量的旋转角度，两个位置的 Q/K 点积会自然带上相对距离信息，所以它比单纯绝对位置编码更适合自回归大模型，也更方便做长上下文扩展。</p></blockquote><h2 id="transformer-深挖追问排查" tabindex="-1">Transformer 深挖追问排查 <a class="header-anchor" href="#transformer-深挖追问排查" aria-label="Permalink to &quot;Transformer 深挖追问排查&quot;">​</a></h2><table tabindex="0"><thead><tr><th>追问</th><th>回答要点</th></tr></thead><tbody><tr><td>Attention 为什么要除以 <code>sqrt(d_k)</code>？</td><td>防止 <code>QK^T</code> 点积随维度增大而过大，softmax 过于尖锐导致梯度不稳定</td></tr><tr><td>Multi-head 为什么有用？</td><td>不同 head 在不同子空间学习不同关系，如局部依赖、实体关系、语法结构</td></tr><tr><td>FFN 在 Transformer 里干什么？</td><td>Attention 做 token 间信息混合，FFN 做每个 token 内部的非线性特征变换</td></tr><tr><td>Residual 为什么重要？</td><td>保留原始信息通路，缓解深层网络梯度消失，便于训练更深模型</td></tr><tr><td>Encoder-only 为什么不适合生成？</td><td>双向注意力会看到未来 token，不符合自回归生成约束</td></tr><tr><td>Decoder-only 为什么也能理解？</td><td>next token prediction 迫使模型学习上下文语义，足够大规模后具备强理解能力</td></tr><tr><td>Embedding 和向量检索 embedding 一样吗？</td><td>不一样。LLM token embedding 服务生成，检索 embedding 服务语义匹配，训练目标不同</td></tr><tr><td>Pre-LN 和 Post-LN 怎么选？</td><td>现代深层 LLM 多用 Pre-LN/RMSNorm，训练更稳定；Post-LN 早期 Transformer 常见</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/transformer/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
