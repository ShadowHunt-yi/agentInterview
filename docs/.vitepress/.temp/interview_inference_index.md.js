import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"推理优化","description":"","frontmatter":{},"headers":[],"relativePath":"interview/inference/index.md","filePath":"interview/inference/index.md","lastUpdated":1780552285000}');
const _sfc_main = { name: "interview/inference/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="推理优化" tabindex="-1">推理优化 <a class="header-anchor" href="#推理优化" aria-label="Permalink to &quot;推理优化&quot;">​</a></h1><h2 id="q8-vllm-的原理理解吗" tabindex="-1">Q8. vLLM 的原理理解吗 <a class="header-anchor" href="#q8-vllm-的原理理解吗" aria-label="Permalink to &quot;Q8. vLLM 的原理理解吗&quot;">​</a></h2><p>vLLM 主要解决大模型在线推理时的吞吐和显存利用问题。核心是 <strong>PagedAttention</strong>：把 KV Cache 像操作系统分页一样管理，不要求一段请求的 KV 在显存里连续存放。</p><p>传统推理的问题：</p><ul><li>不同请求长度不同，KV Cache 容易碎片化。</li><li>为最大长度预分配显存会浪费。</li><li>batch 中请求生成长度不同，调度效率低。</li></ul><p>vLLM 的做法：</p><ul><li>KV Cache 分成固定大小 block。</li><li>请求按 block 映射物理显存。</li><li>支持连续批处理，把新请求动态塞进正在运行的 batch。</li></ul><h2 id="q27-vllm-在推理阶段做了哪些优化" tabindex="-1">Q27. vLLM 在推理阶段做了哪些优化 <a class="header-anchor" href="#q27-vllm-在推理阶段做了哪些优化" aria-label="Permalink to &quot;Q27. vLLM 在推理阶段做了哪些优化&quot;">​</a></h2><p>常见优化点：</p><table tabindex="0"><thead><tr><th>优化</th><th>作用</th></tr></thead><tbody><tr><td>PagedAttention</td><td>减少 KV Cache 显存碎片，提高显存利用率</td></tr><tr><td>Continuous Batching</td><td>动态合批，提高吞吐</td></tr><tr><td>Prefix Cache</td><td>相同前缀复用 KV，适合系统 prompt 或长文档问答</td></tr><tr><td>Tensor Parallel</td><td>多卡切分模型，支撑大模型推理</td></tr><tr><td>Speculative Decoding</td><td>小模型先草稿，大模型验证，加速生成</td></tr></tbody></table><p>面试里重点讲：<strong>vLLM 不是把单个 token 算得更快，而是把服务端批处理和 KV 管理做得更高效</strong>。</p><h2 id="q28-flash-attention-的原理" tabindex="-1">Q28. Flash Attention 的原理 <a class="header-anchor" href="#q28-flash-attention-的原理" aria-label="Permalink to &quot;Q28. Flash Attention 的原理&quot;">​</a></h2><p>Flash Attention 优化的是 attention 的显存读写。普通 attention 会显式生成完整的 <code>QK^T</code> 注意力矩阵，序列长度为 <code>n</code> 时显存是 <code>O(n²)</code>。</p><p>Flash Attention 的核心：</p><ul><li>分块计算 attention。</li><li>在 SRAM 中完成局部 softmax 和累积。</li><li>避免把完整 attention 矩阵写回 HBM。</li><li>使用 online softmax 保持数值稳定。</li></ul><p>效果：</p><ul><li>显著减少显存占用。</li><li>长序列训练和推理更快。</li><li>结果和标准 attention 数学等价或近似等价，取决于实现精度。</li></ul><h2 id="q32-模型量化是怎么量化的" tabindex="-1">Q32. 模型量化是怎么量化的 <a class="header-anchor" href="#q32-模型量化是怎么量化的" aria-label="Permalink to &quot;Q32. 模型量化是怎么量化的&quot;">​</a></h2><p>量化是把高精度权重或激活从 FP16 / BF16 压到 INT8、INT4 等低比特表示。</p><p>基础公式：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>x_int = round(x_float / scale) + zero_point</span></span>
<span class="line"><span>x_float ≈ (x_int - zero_point) × scale</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>常见类型：</p><table tabindex="0"><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td>PTQ</td><td>训练后量化，不重新训练或只用少量校准数据</td></tr><tr><td>QAT</td><td>量化感知训练，训练中模拟量化误差</td></tr><tr><td>Weight-only</td><td>只量化权重，推理更常见</td></tr><tr><td>KV Cache Quantization</td><td>量化 KV Cache，降低长上下文显存</td></tr></tbody></table><p>工程取舍：</p><ul><li>INT8 通常稳定，效果损失小。</li><li>INT4 显存收益更大，但对模型和量化算法更敏感。</li><li>AWQ、GPTQ、bitsandbytes NF4 都是常见路线。</li></ul><h2 id="推理优化深挖追问排查" tabindex="-1">推理优化深挖追问排查 <a class="header-anchor" href="#推理优化深挖追问排查" aria-label="Permalink to &quot;推理优化深挖追问排查&quot;">​</a></h2><h3 id="vllm-为什么能提升吞吐" tabindex="-1">vLLM 为什么能提升吞吐 <a class="header-anchor" href="#vllm-为什么能提升吞吐" aria-label="Permalink to &quot;vLLM 为什么能提升吞吐&quot;">​</a></h3><p>面试里不要只说 PagedAttention，要把推理分成两个阶段讲：</p><table tabindex="0"><thead><tr><th>阶段</th><th>特点</th><th>瓶颈</th></tr></thead><tbody><tr><td>Prefill</td><td>一次性处理 prompt，计算所有 prompt token 的 KV</td><td>计算密集</td></tr><tr><td>Decode</td><td>每次生成 1 个 token，并复用历史 KV</td><td>显存带宽和调度密集</td></tr></tbody></table><p>在线服务真正难的是 decode 阶段：每个请求长度不同，有的已经结束，有的还在生成。如果用固定 batch，很容易出现大量 padding 和等待。</p><p>vLLM 的核心收益来自：</p><ul><li><strong>连续批处理</strong>：请求完成后，新的请求可以立刻进入 batch，不必等整个 batch 都结束。</li><li><strong>PagedAttention</strong>：KV cache 用 block 管理，不要求连续内存，减少碎片。</li><li><strong>更高显存利用率</strong>：同样显存能容纳更多并发请求。</li></ul><p>面试回答：</p><blockquote><p>vLLM 不一定让单个请求首 token 更快，它主要提升服务端吞吐和并发能力。尤其是多用户同时生成时，连续批处理和 KV 分页管理能减少等待和显存浪费。</p></blockquote><h3 id="kv-cache-到底缓存了什么" tabindex="-1">KV Cache 到底缓存了什么 <a class="header-anchor" href="#kv-cache-到底缓存了什么" aria-label="Permalink to &quot;KV Cache 到底缓存了什么&quot;">​</a></h3><p>自回归生成时，第 <code>t</code> 步要关注前面所有 token。如果每一步都重新算历史 token 的 K/V，会非常浪费。</p><p>KV Cache 缓存的是每层 attention 里历史 token 的 <code>K</code> 和 <code>V</code>：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>第 t 步：</span></span>
<span class="line"><span>只计算新 token 的 Q/K/V</span></span>
<span class="line"><span>历史 token 的 K/V 直接从 cache 读取</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>显存占用大致和这些因素成正比：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>layers × sequence_length × hidden_size × batch_size × dtype_size × 2(K,V)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所以长上下文和高并发时，KV Cache 往往比权重更成为瓶颈。</p><h3 id="pagedattention-为什么像操作系统分页" tabindex="-1">PagedAttention 为什么像操作系统分页 <a class="header-anchor" href="#pagedattention-为什么像操作系统分页" aria-label="Permalink to &quot;PagedAttention 为什么像操作系统分页&quot;">​</a></h3><p>传统 KV Cache 像给每个请求分配一大段连续显存，问题是请求长度不确定，容易浪费和碎片化。</p><p>PagedAttention 把 KV Cache 切成固定大小 block：</p><ul><li>逻辑上：每个请求有自己的 token 序列。</li><li>物理上：这些 token 的 KV 分散存在多个 block 中。</li><li>映射表：记录请求的逻辑 token 对应哪些物理 block。</li></ul><p>这样做的好处：</p><ul><li>不用提前给最大长度分配完整显存。</li><li>请求结束后 block 可以回收。</li><li>多个请求之间可以更灵活地调度。</li></ul><h3 id="flash-attention-为什么不是简单少算" tabindex="-1">Flash Attention 为什么不是简单少算 <a class="header-anchor" href="#flash-attention-为什么不是简单少算" aria-label="Permalink to &quot;Flash Attention 为什么不是简单少算&quot;">​</a></h3><p>Flash Attention 并没有改变 attention 的数学结果，它主要减少 HBM 显存读写。</p><p>普通 attention 的问题：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>QK^T 会生成 n × n 的注意力矩阵</span></span>
<span class="line"><span>这个矩阵很大，需要频繁读写 HBM</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Flash Attention 的思路：</p><ul><li>把 Q/K/V 分块搬进更快的 SRAM。</li><li>在块内计算局部 attention。</li><li>用 online softmax 维护全局 softmax 的数值稳定性。</li><li>不把完整 attention matrix 写回 HBM。</li></ul><p>追问回答：</p><blockquote><p>Flash Attention 快，不是因为少算了 attention，而是因为减少了高成本显存访问。GPU 上很多时候瓶颈不是算力，而是 HBM 带宽。</p></blockquote><h3 id="online-softmax-为什么能保持正确" tabindex="-1">Online Softmax 为什么能保持正确 <a class="header-anchor" href="#online-softmax-为什么能保持正确" aria-label="Permalink to &quot;Online Softmax 为什么能保持正确&quot;">​</a></h3><p>分块计算时，不能直接对每个块单独 softmax 后拼起来，因为 softmax 分母应该是全局所有 token 的指数和。</p><p>Online softmax 会维护两个量：</p><ul><li>当前看到的最大值 <code>m</code>。</li><li>当前归一化分母 <code>l</code>。</li></ul><p>每来一个新 block，就更新全局最大值和分母，从而得到和完整 softmax 等价的结果。</p><p>面试里不用推完整公式，说清楚：</p><blockquote><p>它不是局部 softmax，而是边分块边维护全局 softmax 的最大值和归一化项，所以数值上仍然等价。</p></blockquote><h3 id="量化误差从哪里来" tabindex="-1">量化误差从哪里来 <a class="header-anchor" href="#量化误差从哪里来" aria-label="Permalink to &quot;量化误差从哪里来&quot;">​</a></h3><p>量化不是单纯把 FP16 改成 INT4，它会引入近似误差：</p><table tabindex="0"><thead><tr><th>来源</th><th>说明</th></tr></thead><tbody><tr><td>scale 粒度</td><td>per-tensor 粗，per-channel / per-group 更准</td></tr><tr><td>outlier</td><td>少数极大权重会拉大 scale，让普通值精度变差</td></tr><tr><td>激活分布</td><td>activation 量化比 weight-only 更难</td></tr><tr><td>校准数据</td><td>PTQ 依赖校准集，分布不匹配会掉点</td></tr></tbody></table><p>常见回答：</p><blockquote><p>INT4 省显存明显，但不是所有层都同样适合量化。工程上会用 group-wise quantization、保护 outlier、或者只量化权重，来平衡效果和性能。</p></blockquote><h3 id="推理优化追问清单" tabindex="-1">推理优化追问清单 <a class="header-anchor" href="#推理优化追问清单" aria-label="Permalink to &quot;推理优化追问清单&quot;">​</a></h3><table tabindex="0"><thead><tr><th>追问</th><th>回答要点</th></tr></thead><tbody><tr><td>vLLM 优化首 token 还是吞吐？</td><td>主要优化吞吐和并发，prefill 首 token 还受模型计算影响</td></tr><tr><td>PagedAttention 解决什么？</td><td>KV Cache 显存碎片和预分配浪费</td></tr><tr><td>Continuous batching 为什么有效？</td><td>动态把新请求塞进 decode batch，减少等待</td></tr><tr><td>Flash Attention 改了数学公式吗？</td><td>不改公式，主要减少 HBM 读写</td></tr><tr><td>量化为什么会掉效果？</td><td>低比特近似、outlier、scale 粒度和校准数据都会带来误差</td></tr><tr><td>INT4 一定比 INT8 快吗？</td><td>不一定，还要看 kernel、硬件支持、反量化开销和 batch size</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/inference/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
