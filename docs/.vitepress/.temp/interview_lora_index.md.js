import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"LoRA 微调","description":"","frontmatter":{},"headers":[],"relativePath":"interview/lora/index.md","filePath":"interview/lora/index.md","lastUpdated":1780552285000}');
const _sfc_main = { name: "interview/lora/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="lora-微调" tabindex="-1">LoRA 微调 <a class="header-anchor" href="#lora-微调" aria-label="Permalink to &quot;LoRA 微调&quot;">​</a></h1><h2 id="q1-讲一下-lora-原理" tabindex="-1">Q1. 讲一下 LoRA 原理 <a class="header-anchor" href="#q1-讲一下-lora-原理" aria-label="Permalink to &quot;Q1. 讲一下 LoRA 原理&quot;">​</a></h2><p>LoRA 的核心思想是：<strong>冻结原模型权重，只训练一个低秩的增量更新</strong>。原本全量微调要直接更新权重矩阵 <code>W</code>，LoRA 改成在原线性层旁边加一个低秩旁路：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>W&#39; = W + ΔW</span></span>
<span class="line"><span>ΔW = B × A</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>其中 <code>A</code> 和 <code>B</code> 是两个小矩阵，rank 为 <code>r</code>，通常远小于原矩阵维度。这样训练参数量从 <code>d_in × d_out</code> 降到 <code>r × (d_in + d_out)</code>。</p><p>更贴近 forward 的写法是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>y = W x + scale · B A x</span></span>
<span class="line"><span>scale = lora_alpha / r</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>如果原线性层是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>W: d_out × d_in</span></span>
<span class="line"><span>x: d_in</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>那么常见 LoRA 维度是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A: r × d_in</span></span>
<span class="line"><span>B: d_out × r</span></span>
<span class="line"><span>B A: d_out × d_in</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>B A</code> 的形状和原来的 <code>W</code> 一样，所以可以作为 <code>W</code> 的增量。</p><p>面试里可以这样说：</p><blockquote><p>LoRA 认为大模型在特定任务上的权重更新往往是低秩的，所以不必训练完整权重矩阵。它用两个低秩矩阵近似权重增量，推理时把增量加回原权重，达到低成本适配任务的效果。</p></blockquote><h3 id="a-b-矩阵怎么初始化" tabindex="-1">A/B 矩阵怎么初始化 <a class="header-anchor" href="#a-b-矩阵怎么初始化" aria-label="Permalink to &quot;A/B 矩阵怎么初始化&quot;">​</a></h3><p>LoRA 论文和常见 PEFT 实现里，典型初始化是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A: 随机初始化</span></span>
<span class="line"><span>B: 全 0 初始化</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>也就是训练一开始：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ΔW = B × A = 0 × A = 0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所以初始模型输出完全等于原模型：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>y = W x</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这点很关键，因为 LoRA 是在一个已经训练好的大模型上加适配器。初始化时如果立刻随机扰动原模型输出，会让训练初期不稳定。</p><h3 id="为什么-b-可以初始化为-0" tabindex="-1">为什么 B 可以初始化为 0 <a class="header-anchor" href="#为什么-b-可以初始化为-0" aria-label="Permalink to &quot;为什么 B 可以初始化为 0&quot;">​</a></h3><p>因为只让 <code>B</code> 为 0，并不会让整个 LoRA 分支彻底“死掉”。看梯度就能解释：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Δy = B A x</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>训练第一步时：</p><ul><li><code>A</code> 是随机的，所以 <code>A x</code> 通常不是 0。</li><li><code>B</code> 虽然是 0，但 <code>B</code> 的梯度依赖 <code>A x</code>，因此 <code>B</code> 可以收到非零梯度。</li><li>第一轮更新后，<code>B</code> 不再是 0，之后 <code>A</code> 也会开始收到有效梯度。</li></ul><p>更直观地说：</p><blockquote><p>B=0 只是保证 LoRA 初始不改变原模型输出；A 随机提供了一个可学习的低秩子空间，所以梯度仍然能先推动 B 学起来。</p></blockquote><h3 id="为什么不能-a-和-b-都初始化为-0" tabindex="-1">为什么不能 A 和 B 都初始化为 0 <a class="header-anchor" href="#为什么不能-a-和-b-都初始化为-0" aria-label="Permalink to &quot;为什么不能 A 和 B 都初始化为 0&quot;">​</a></h3><p>如果 <code>A=0</code> 且 <code>B=0</code>：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Δy = B A x = 0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这时两个矩阵互相依赖：</p><ul><li><code>B</code> 的梯度里有 <code>A x</code>，但 <code>A=0</code>。</li><li><code>A</code> 的梯度里有 <code>B</code>，但 <code>B=0</code>。</li></ul><p>结果就是两边梯度都可能为 0，LoRA 分支学不起来。</p><p>所以面试里要说清楚：</p><blockquote><p>不是“LoRA 矩阵都可以是 0”，而是“只把其中一个矩阵置 0，另一个保持随机”，这样既不扰动原模型，又保留梯度通路。</p></blockquote><h3 id="为什么不是两个矩阵都随机初始化" tabindex="-1">为什么不是两个矩阵都随机初始化 <a class="header-anchor" href="#为什么不是两个矩阵都随机初始化" aria-label="Permalink to &quot;为什么不是两个矩阵都随机初始化&quot;">​</a></h3><p>如果 <code>A</code> 和 <code>B</code> 都随机：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ΔW = B × A ≠ 0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>训练一开始 LoRA 就会给原模型输出加随机扰动。这对预训练模型不友好，尤其是大模型已经有很强的语言能力，随机 adapter 会破坏原本的输出分布。</p><p>所以常见初始化策略本质上是在平衡两件事：</p><table tabindex="0"><thead><tr><th>目标</th><th>做法</th></tr></thead><tbody><tr><td>初始不破坏原模型</td><td>让 <code>B=0</code>，保证 <code>ΔW=0</code></td></tr><tr><td>保证能训练起来</td><td>让 <code>A</code> 随机，给 <code>B</code> 提供非零梯度方向</td></tr></tbody></table><h3 id="为什么用低秩矩阵就够了" tabindex="-1">为什么用低秩矩阵就够了 <a class="header-anchor" href="#为什么用低秩矩阵就够了" aria-label="Permalink to &quot;为什么用低秩矩阵就够了&quot;">​</a></h3><p>LoRA 的假设是：下游任务对大模型权重的改动不需要覆盖完整参数空间，很多变化集中在一个低维子空间里。</p><p>例如原矩阵有几千万参数，但某个客服问答任务可能只需要调整：</p><ul><li>某些注意力模式；</li><li>某些领域术语表达；</li><li>某些回答风格；</li><li>某些工具调用格式。</li></ul><p>这些变化往往可以用 rank 很小的 <code>ΔW</code> 表示。rank 越大，可表达的更新空间越大；rank 越小，越省参数但表达能力受限。</p><h3 id="推理时-lora-怎么处理" tabindex="-1">推理时 LoRA 怎么处理 <a class="header-anchor" href="#推理时-lora-怎么处理" aria-label="Permalink to &quot;推理时 LoRA 怎么处理&quot;">​</a></h3><p>LoRA 训练完后有两种推理方式：</p><ol><li><strong>不合并</strong>：保留 <code>W x + B A x</code> 两条分支，方便动态切换 adapter。</li><li><strong>合并权重</strong>：把 <code>ΔW = scale · B A</code> 加回 <code>W</code>，得到新的 <code>W&#39;</code>。</li></ol><p>合并后：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>W&#39; = W + scale · B A</span></span>
<span class="line"><span>y = W&#39; x</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这样推理时就不需要额外 LoRA 分支，延迟基本和原模型一致。多 adapter 动态切换时通常不合并，单任务部署时可以合并。</p><h3 id="这题的追问排查清单" tabindex="-1">这题的追问排查清单 <a class="header-anchor" href="#这题的追问排查清单" aria-label="Permalink to &quot;这题的追问排查清单&quot;">​</a></h3><table tabindex="0"><thead><tr><th>追问</th><th>回答要点</th></tr></thead><tbody><tr><td>A/B 谁是 0？</td><td>常见实现是 <code>A</code> 随机，<code>B</code> 为 0</td></tr><tr><td>为什么 B 可以是 0？</td><td>初始 <code>ΔW=0</code> 不扰动原模型，但 <code>B</code> 仍能通过随机 <code>A</code> 收到梯度</td></tr><tr><td>为什么不能两个都 0？</td><td>两边梯度都会被对方的 0 卡住，分支学不起来</td></tr><tr><td>为什么不两个都随机？</td><td>初始会随机扰动预训练模型输出，训练不稳定</td></tr><tr><td><code>alpha / r</code> 是什么？</td><td>控制 LoRA 增量强度，避免 rank 改变时更新尺度失控</td></tr><tr><td>推理是否更慢？</td><td>合并 LoRA 后不增加线性层计算；不合并时会多一条低秩分支</td></tr><tr><td>LoRA 和全量微调区别？</td><td>全量改 <code>W</code>，LoRA 冻结 <code>W</code> 只学低秩 <code>ΔW</code></td></tr></tbody></table><h2 id="q2-lora-挂在哪层" tabindex="-1">Q2. LoRA 挂在哪层 <a class="header-anchor" href="#q2-lora-挂在哪层" aria-label="Permalink to &quot;Q2. LoRA 挂在哪层&quot;">​</a></h2><p>最常见挂在 Transformer 的线性投影层：</p><table tabindex="0"><thead><tr><th>位置</th><th>常见模块名</th><th>作用</th></tr></thead><tbody><tr><td>Attention</td><td><code>q_proj</code>、<code>k_proj</code>、<code>v_proj</code>、<code>o_proj</code></td><td>改变注意力查询、匹配、聚合和输出</td></tr><tr><td>MLP</td><td><code>gate_proj</code>、<code>up_proj</code>、<code>down_proj</code></td><td>增强任务表达和非线性变换能力</td></tr><tr><td>Embedding / LM Head</td><td>较少使用</td><td>词表或输出分布适配，风险和成本更高</td></tr></tbody></table><p>工程上常用策略：</p><ul><li>资源紧张或数据少：优先挂 <code>q_proj</code>、<code>v_proj</code>。</li><li>想要效果更强：挂 <code>q/k/v/o</code>。</li><li>任务风格变化大：再加 MLP 的 <code>gate/up/down</code>。</li></ul><h2 id="q3-lora-有哪些参数配置-会影响什么" tabindex="-1">Q3. LoRA 有哪些参数配置，会影响什么 <a class="header-anchor" href="#q3-lora-有哪些参数配置-会影响什么" aria-label="Permalink to &quot;Q3. LoRA 有哪些参数配置，会影响什么&quot;">​</a></h2><table tabindex="0"><thead><tr><th>参数</th><th>含义</th><th>影响</th></tr></thead><tbody><tr><td><code>r</code> / <code>rank</code></td><td>低秩矩阵维度</td><td>越大拟合能力越强，显存和过拟合风险也越高</td></tr><tr><td><code>lora_alpha</code></td><td>LoRA 缩放系数</td><td>控制增量权重强度，常见有效缩放是 <code>alpha / r</code></td></tr><tr><td><code>lora_dropout</code></td><td>LoRA 分支 dropout</td><td>抑制过拟合，数据少时更有用</td></tr><tr><td><code>target_modules</code></td><td>挂载模块</td><td>决定改模型的哪些能力</td></tr><tr><td><code>bias</code></td><td>是否训练 bias</td><td>一般设 <code>none</code>，稳定且省参数</td></tr><tr><td><code>task_type</code></td><td>任务类型</td><td>Causal LM、Seq2Seq、分类等</td></tr><tr><td><code>modules_to_save</code></td><td>额外保存模块</td><td>常用于保存 <code>lm_head</code> 或分类头</td></tr><tr><td><code>init_lora_weights</code></td><td>LoRA 初始化方式</td><td>常见是 <code>A</code> 随机、<code>B</code> 置 0，保证初始不扰动原模型</td></tr><tr><td><code>use_rslora</code></td><td>Rank-Stabilized LoRA</td><td>常用 <code>alpha / sqrt(r)</code> 缩放，rank 较大时更新尺度更稳</td></tr></tbody></table><h3 id="参数之间怎么联动" tabindex="-1">参数之间怎么联动 <a class="header-anchor" href="#参数之间怎么联动" aria-label="Permalink to &quot;参数之间怎么联动&quot;">​</a></h3><p>面试里不要把参数孤立地背，要说它们的联动关系：</p><ul><li><code>r</code> 决定低秩空间大小，影响拟合能力和参数量。</li><li><code>lora_alpha</code> 决定 LoRA 更新强度，通常和 <code>r</code> 搭配看。</li><li><code>target_modules</code> 决定改哪些能力，比单纯调大 rank 更关键。</li><li><code>lora_dropout</code> 主要用于防过拟合，数据少时更有价值。</li><li><code>init_lora_weights</code> 决定训练起点是否扰动原模型。</li></ul><p>常见追问：</p><blockquote><p>如果 rank 从 8 提到 64，但 alpha 不变，传统 <code>alpha / r</code> 缩放会让每个 rank 的平均更新变弱；如果 alpha 也跟着调大，LoRA 分支影响会增强。调参时要同时看 rank、alpha 和验证集效果。</p></blockquote><h2 id="q23-lora-适配器和原模型尺寸有什么关系" tabindex="-1">Q23. LoRA 适配器和原模型尺寸有什么关系 <a class="header-anchor" href="#q23-lora-适配器和原模型尺寸有什么关系" aria-label="Permalink to &quot;Q23. LoRA 适配器和原模型尺寸有什么关系&quot;">​</a></h2><p>LoRA 适配器尺寸和原矩阵输入输出维度、rank 有关。假设原线性层权重是 <code>d_out × d_in</code>，LoRA 参数量约为：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>r × d_in + d_out × r = r × (d_in + d_out)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>所以模型越大、挂载层越多、rank 越高，适配器越大。但相比全量权重，LoRA 通常只占原模型参数量的很小一部分。</p><h2 id="q24-哪个参数影响拟合能力" tabindex="-1">Q24. 哪个参数影响拟合能力 <a class="header-anchor" href="#q24-哪个参数影响拟合能力" aria-label="Permalink to &quot;Q24. 哪个参数影响拟合能力&quot;">​</a></h2><p>最直接的是 <code>rank</code>。rank 越大，低秩增量矩阵能表达的变化越复杂，拟合能力越强。其次是 <code>target_modules</code>，挂载更多模块也会提升可训练参数量和表达能力。</p><p>面试追问可以补：</p><blockquote><p>rank 不是越大越好。数据量小的时候 rank 太大容易记忆训练集，泛化变差；数据足够、任务复杂时可以提高 rank 或扩展 target modules。</p></blockquote><h2 id="q25-rank-和两个小矩阵有什么关系" tabindex="-1">Q25. Rank 和两个小矩阵有什么关系 <a class="header-anchor" href="#q25-rank-和两个小矩阵有什么关系" aria-label="Permalink to &quot;Q25. Rank 和两个小矩阵有什么关系&quot;">​</a></h2><p>rank 就是两个小矩阵中间的瓶颈维度：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A: r × d_in</span></span>
<span class="line"><span>B: d_out × r</span></span>
<span class="line"><span>ΔW = B × A</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>r</code> 越小，<code>ΔW</code> 的秩上限越低，表达更受限制；<code>r</code> 越大，越接近全量更新，但训练成本也会上升。</p><p>注意：不同论文、框架或讲法里，<code>A/B</code> 的命名可能反过来。面试时不要只背名字，重点讲清楚：</p><ul><li>一个矩阵负责把 <code>d_in</code> 降到 <code>r</code>。</li><li>另一个矩阵负责把 <code>r</code> 升回 <code>d_out</code>。</li><li>两者乘积的形状必须和原权重 <code>W</code> 一致。</li><li>常见初始化是“降维矩阵随机、升维矩阵置 0”，但具体叫 <code>A</code> 还是 <code>B</code> 要看框架约定。</li></ul><h2 id="q26-lora-微调用什么算力卡" tabindex="-1">Q26. LoRA 微调用什么算力卡 <a class="header-anchor" href="#q26-lora-微调用什么算力卡" aria-label="Permalink to &quot;Q26. LoRA 微调用什么算力卡&quot;">​</a></h2><p>面试不一定要说固定型号，重点说清楚依据：</p><ul><li>7B 模型：单张 24GB 卡配合 QLoRA 通常可做 SFT。</li><li>14B 模型：更稳妥是 48GB 或多卡。</li><li>32B 以上：通常需要多卡、ZeRO、FSDP 或更激进量化。</li></ul><p>可用话术：</p><blockquote><p>我会先看模型规模、序列长度、batch size 和是否用 QLoRA。LoRA 本身省的是可训练参数和优化器状态，但前向激活和 KV 仍然占显存，所以长上下文训练时显存压力仍然明显。</p></blockquote><h2 id="q29-lora-参数怎么设置-为什么" tabindex="-1">Q29. LoRA 参数怎么设置，为什么 <a class="header-anchor" href="#q29-lora-参数怎么设置-为什么" aria-label="Permalink to &quot;Q29. LoRA 参数怎么设置，为什么&quot;">​</a></h2><p>一个保守起步配置：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>r = 8 或 16</span></span>
<span class="line"><span>lora_alpha = 16 或 32</span></span>
<span class="line"><span>lora_dropout = 0.05</span></span>
<span class="line"><span>target_modules = q_proj, k_proj, v_proj, o_proj</span></span>
<span class="line"><span>bias = none</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>调整思路：</p><ul><li>欠拟合：提高 <code>r</code>，扩大 <code>target_modules</code> 到 MLP。</li><li>过拟合：降低 <code>r</code>，提高 dropout，减少 epoch。</li><li>风格迁移强：适当提高 alpha 或挂 MLP。</li><li>事实问答类：更关注数据质量和防遗忘，不盲目加 rank。</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/lora/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
